<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="Tieude">
        <!-- Customize your theme here. -->
        <item name="android:textColor">#fcdb37</item>
        <item name="android:textSize">30dp</item>
        <item name="android:background">@drawable/title</item>
    </style>
    <style name="txtNhap">
        <!-- Customize your theme here. -->
        <item name="android:inputType">number</item>
        <item name="android:background">@drawable/txtnhap</item>
        <item name="android:gravity">center</item>
        <item name="android:textColor">#F41ED7</item>
        <item name="android:textSize">20dp</item>
        <item name="android:padding">5dp</item>
        <item name="android:layout_margin">5dp</item>
    </style>

    <style name="pheptinh">
        <!-- Customize your theme here. -->
        <item name="android:background">@drawable/btntinh</item>
        <item name="android:layout_margin">5dp</item>

    </style>
</resources>
